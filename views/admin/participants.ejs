<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= eventName %> | Participants</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white p-8 min-h-screen">
  <h1 class="text-3xl font-bold text-green-400 mb-6">
    Participants for <%= eventName %>
  </h1>

  <% if (participants.length === 0) { %>
    <p class="text-gray-400 text-lg">No participants registered yet.</p>
  <% } else { %>
    <div class="overflow-x-auto">
      <table class="min-w-full border border-gray-700 rounded-xl">
        <thead class="bg-gray-800 text-green-300">
          <tr>
            <th class="p-3 text-left">Name</th>
            <th class="p-3 text-left">Contact</th>
            <th class="p-3 text-left">Email</th>
            <th class="p-3 text-left">Registered At</th>
          </tr>
        </thead>
        <tbody>
          <% participants.forEach(p => { %>
            <tr class="border-t border-gray-700 hover:bg-gray-800/60 transition">
              <td class="p-3"><%= p.name %></td>
              <td class="p-3"><%= p.contact %></td>
              <td class="p-3"><%= p.email %></td>
              <td class="p-3"><%= new Date(p.registeredAt).toLocaleString() %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>

  <div class="mt-6">
    <a href="/admin/ongoing-events" class="bg-gray-800 text-green-400 px-5 py-2 rounded-lg hover:bg-gray-700 transition">
      ← Back to Events
    </a>
  </div>
</body>
</html>
